# Failure Analysis

This directory contains failure-analysis work for joinability discovery.

Current analysis workspace:

- `deepjoin/`: DeepJoin-style column embedding failure analysis on LakeBench.

Old generated reports, plots, samples, and local artifacts were removed so the
next analysis can start from a clean workspace.
