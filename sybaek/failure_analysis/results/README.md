# Results

This directory keeps compact, reviewable outputs for GitHub.

Committed outputs:

- `failure_group_summary.csv`
- `webtable_join_prepare_summary.csv`
- `samples/*.csv`
- `plots/*.png`

Ignored but reproducible outputs:

- `column_profiles.csv`
- `pair_overlap_metrics.csv`
- `rank_comparison.csv`
- `failure_table.csv`
- `webtable_join_prepare_skipped.csv`

Large raw files, embeddings, indexes, model checkpoints, and caches should stay in `../artifacts/`, `../cache/`, or `../logs/`.
